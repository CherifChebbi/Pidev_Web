<form method="post" action="{{ path('app_pays_delete', {'id_pays': pay.idPays}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pay.idPays) }}">
    <button class="btn bg-gradient-warning">Delete</button>
</form>
