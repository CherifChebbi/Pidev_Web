<!DOCTYPE html>

<title>Pays</title>

{% block body %}
    <h1>Pays</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_pays</th>
                <td>{{ pay.idPays }}</td>
            </tr>
            <tr>
                <th>Nom_pays</th>
                <td>{{ pay.nomPays }}</td>
            </tr>
            <tr>
                <th>Img_pays</th>
                <td>{{ pay.imgPays }}</td>
            </tr>
            <tr>
                <th>Desc_pays</th>
                <td>{{ pay.descPays }}</td>
            </tr>
            <tr>
                <th>Langue</th>
                <td>{{ pay.langue }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pays_index') }}">back to list</a>

    <a href="{{ path('app_pays_edit', {'id_pays': pay.id_pays}) }}">edit</a>

    {{ include('pays/_delete_form.html.twig') }}
{% endblock %}
