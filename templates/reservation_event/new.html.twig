{% extends 'back/t_base_events.html.twig' %}

{% block title %}Create new ReservationEvent{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">Créer une nouvelle réservation </h3>
                    </div>
                    <div class="card-body">
                        {{ include('reservation_event/_form.html.twig') }}
                    </div>
                    <div class="card-footer">
                        <a href="{{ path('app_reservation_event_index') }}" class="btn btn-secondary">Back to list</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
