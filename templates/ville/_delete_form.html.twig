<form method="post" action="{{ path('app_ville_delete', {'id_ville': ville.idVille}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ville.idVille) }}">
    <button class="btn bg-gradient-warning">Delete</button>
</form>
